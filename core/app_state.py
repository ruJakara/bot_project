"""Global app state — simplified for single-bot (no multi-tenant)."""
from __future__ import annotations

# This module is kept for import compatibility.
# Multi-tenant functions have been removed.
# New code should use config.get_settings() directly.
